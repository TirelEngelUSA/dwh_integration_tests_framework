        excluded = {'check_rules.sql', 'post_deploy.sql'}
        script_names = [
            s for s in (script_names if isinstance(script_names, list) else [script_names])
            if isinstance(s, str) and s.strip() and s.strip().lower() not in excluded
        ]
